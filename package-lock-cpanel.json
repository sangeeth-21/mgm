
{
  "name": "specd-website",
  "version": "1.0.0",
  "lockfileVersion": 2,
  "requires": true,
  "packages": {
    "": {
      "name": "specd-website",
      "version": "1.0.0",
      "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "deploy": "npm run build && echo 'Build complete. Upload dist folder contents to cPanel public_html directory.'"
      }
    }
  }
}
